# No external dependencies required.
# avian.py uses only Python standard library modules:
# - json, hashlib, subprocess, os, argparse, time, base64, fcntl, struct, select, socket
# 
# Python version: >=3.6
# 
# System dependencies (install via package manager, e.g., apt):
# - iproute2 (for 'ip' command)
# - tcpdump (for debugging)
# - tar (for mounting)
# - bash (for command execution)
# 
# Kernel: Linux with TUN/TAP support (modprobe tun)
